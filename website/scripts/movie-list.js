// The MovieList Class


class MovieList {
    constructor(rootId, movies) {
        this.rootId = rootId // the html id of there the list is to be shown
        this.movies = movies; // the array of movies to be displayed
        this.refresh();
    } // End of the constructor

    // Methods
    // Generate one row of the list
    // Generate all rows
    // Generate the movieList based on search term

    // Remove all list element from the DOM
    // Refresh function
    // Add function
    // Update function
    // Delete function

    // Sort A - Z
    // Sort Z - A
    // Sort Rating

    // Search by partial title
    
}

